{
  "name": "video-sync-challenge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"server,web\" -c \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix server && npm run build --prefix web",
    "start": "concurrently -n \"server,web\" -c \"cyan,magenta\" \"npm run start --prefix server\" \"npm run start --prefix web\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
